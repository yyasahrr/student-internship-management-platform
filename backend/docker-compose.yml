# کارآموزیار — Docker Compose
# Usage: docker compose up -d
# Or:    docker-compose up -d

services:
  # ---------- PostgreSQL Database ---------- #
  db:
    image: postgres:16-alpine
    container_name: karamoozyar-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: karamoozyar
      POSTGRES_USER: karamoozyar
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U karamoozyar -d karamoozyar"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ---------- Django Backend API ---------- #
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: karamoozyar-api
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - POSTGRES_HOST=db
      - POSTGRES_DB=karamoozyar
      - POSTGRES_USER=karamoozyar
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD}
      - POSTGRES_PORT=5432
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - media_data:/app/media
    command: >
      bash -c "
        python manage.py migrate --noinput &&
        python manage.py collectstatic --noinput &&
        gunicorn config.wsgi:application --bind 0.0.0.0:8000 --workers 3 --timeout 60 --access-logfile -
      "

volumes:
  pgdata:
    driver: local
  media_data:
    driver: local
