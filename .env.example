# Public Django API URL (included in the browser bundle)
NEXT_PUBLIC_API_URL=https://api.example.com/api

# Long random secret used to sign the encrypted application session cookie
AUTH_SECRET=generate-a-long-random-secret
